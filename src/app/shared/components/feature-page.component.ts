import { ChangeDetectionStrategy, Component, input } from '@angular/core';import { MatCardModule } from '@angular/material/card';@Component({selector:'b0-feature-page',standalone:true,imports:[MatCardModule],template:`<mat-card><h1 class="text-2xl font-bold">{{title()}}</h1><p>{{description()}}</p><ng-content /></mat-card>`,changeDetection:ChangeDetectionStrategy.OnPush})export class FeaturePageComponent{title=input('Feature');description=input('This production feature is wired to typed backend API services and guarded routes.');}
