import { inject, Injectable } from '@angular/core';import { finalize, Observable, shareReplay, tap } from 'rxjs';import { ApiService } from '../api/api.service';import { AuthResponse, LoginRequest, RegisterRequest } from '../api/api.types';import { AuthStore } from './auth.store';
@Injectable({providedIn:'root'}) export class AuthService{#api=inject(ApiService);#store=inject(AuthStore);#refresh$?:Observable<AuthResponse>;login(req:LoginRequest){return this.#api.post<AuthResponse>('/auth/login',req).pipe(tap(r=>this.#store.setSession(r.accessToken,r.user)));}register(req:RegisterRequest){return this.#api.post<AuthResponse>('/auth/register',req).pipe(tap(r=>this.#store.setSession(r.accessToken,r.user)));}logout(){this.#store.clear();return this.#api.post<void>('/auth/logout',{});}forgotPassword(email:string){return this.#api.post<void>('/auth/forgot-password',{email});}resetPassword(token:string,password:string){return this.#api.post<void>('/auth/reset-password',{token,password});}verifyEmail(token:string){return this.#api.post<void>('/auth/verify-email',{token});}loadProfile(){return this.#api.get<AuthResponse>('/auth/me').pipe(tap(r=>this.#store.setSession(r.accessToken,r.user)));}refreshToken(){if(!this.#refresh$){this.#refresh$=this.#api.post<AuthResponse>('/auth/refresh',{}).pipe(tap(r=>this.#store.setSession(r.accessToken,r.user)),shareReplay(1),finalize(()=>this.#refresh$=undefined));}return this.#refresh$;}}
