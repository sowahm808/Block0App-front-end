import { ChangeDetectionStrategy, Component, inject } from '@angular/core';import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';import { Router, RouterLink } from '@angular/router';import { MatButtonModule } from '@angular/material/button';import { MatCardModule } from '@angular/material/card';import { MatFormFieldModule } from '@angular/material/form-field';import { MatInputModule } from '@angular/material/input';import { AuthService } from '../../core/auth/auth.service';
@Component({standalone:true,imports:[ReactiveFormsModule,RouterLink,MatButtonModule,MatCardModule,MatFormFieldModule,MatInputModule],template:`<mat-card class="mx-auto max-w-md"><mat-card-title>Login</mat-card-title><mat-card-content><form [formGroup]="form" (ngSubmit)="submit()" class="grid gap-3"><mat-form-field><mat-label>Email</mat-label><input matInput formControlName="email" autocomplete="email"></mat-form-field><mat-form-field><mat-label>Password</mat-label><input matInput type="password" formControlName="password" autocomplete="current-password"></mat-form-field><button mat-raised-button color="primary" [disabled]="form.invalid||busy">Login</button><a routerLink="/forgot-password">Forgot password?</a></form></mat-card-content></mat-card>`,changeDetection:ChangeDetectionStrategy.OnPush})export class LoginPage{#fb=inject(FormBuilder);#auth=inject(AuthService);#router=inject(Router);busy=false;form=this.#fb.nonNullable.group({email:['',[Validators.required,Validators.email]],password:['',Validators.required]});submit(){if(this.form.invalid)return;this.busy=true;this.#auth.login(this.form.getRawValue()).subscribe({next:()=>void this.#router.navigateByUrl('/dashboard'),error:()=>this.busy=false});}}
@Component({standalone:true,imports:[ReactiveFormsModule,MatButtonModule,MatCardModule,MatFormFieldModule,MatInputModule],template:`<mat-card class="mx-auto max-w-md"><mat-card-title>Register</mat-card-title><form [formGroup]="form" (ngSubmit)="submit()" class="grid gap-3 p-4"><mat-form-field><mat-label>Name</mat-label><input matInput formControlName="displayName"></mat-form-field><mat-form-field><mat-label>Email</mat-label><input matInput formControlName="email"></mat-form-field><mat-form-field><mat-label>Password</mat-label><input matInput type="password" formControlName="password"></mat-form-field><button mat-raised-button color="primary" [disabled]="form.invalid">Create account</button></form></mat-card>`,changeDetection:ChangeDetectionStrategy.OnPush})export class RegisterPage{#fb=inject(FormBuilder);#auth=inject(AuthService);#router=inject(Router);form=this.#fb.nonNullable.group({displayName:['',Validators.required],email:['',[Validators.required,Validators.email]],password:['',[Validators.required,Validators.minLength(12)]]});submit(){if(this.form.valid)this.#auth.register(this.form.getRawValue()).subscribe(()=>void this.#router.navigateByUrl('/dashboard'));}}
@Component({standalone:true,template:`<h1>{{title}}</h1><p>Use the secure email flow from the backend to continue.</p>`})export class SimpleAuthPage{title='Account access';}
