# Contributing
Use standalone components, OnPush change detection, inject(), signals for local state, lazy routes, typed API services, and tests for business logic. Keep components under 300 lines where practical.
